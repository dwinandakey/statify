diff --git a/node_modules/sav-reader/dist/SavMetaLoader.js b/node_modules/sav-reader/dist/SavMetaLoader.js
index f0d1967..0f9353b 100644
--- a/node_modules/sav-reader/dist/SavMetaLoader.js
+++ b/node_modules/sav-reader/dist/SavMetaLoader.js
@@ -35,6 +35,7 @@ class SavMetaLoader {
             let longVariableNamesMap;
             let longStringVarsMap = null;
             let valueLabelsExt = null;
+            let auxVarParams = null;
             let done = false;
             do {
                 const rec_type = yield reader.peekInt();
@@ -89,6 +90,9 @@ class SavMetaLoader {
                     else if (rec.subType === RecordType_js_1.InfoRecordSubType.StringVariableValueLabelsRecord) {
                         valueLabelsExt = rec;
                     }
+                    else if (rec.subType === RecordType_js_1.InfoRecordSubType.AuxilliaryVariableParameter) {
+                        auxVarParams = rec;
+                    }
                 }
                 else if (rec_type === RecordType_js_1.RecordType.DictionaryTerminationRecord) {
                     yield reader.readInt32(); // consume peeked record type
@@ -180,6 +184,25 @@ class SavMetaLoader {
                 delete set2._appliesToShortNames;
                 return set2;
             });
+            // Assign measurement level (nominal/ordinal/scale) from the
+            // Auxiliary Variable Parameter info record, if present. This
+            // record stores 3 int32s per variable, in dictionary order:
+            // [measurementLevel, displayWidth, alignment]. Verified against
+            // real .sav files (including ones with long string variables)
+            // that its count always equals meta.sysvars.length * 3, i.e. it
+            // aligns with the fully resolved variable list, not the raw
+            // variable-record count.
+            if (auxVarParams && Array.isArray(auxVarParams.values) &&
+                auxVarParams.values.length === meta.sysvars.length * 3) {
+                const measureCodeMap = { 1: 'nominal', 2: 'ordinal', 3: 'scale' };
+                meta.sysvars.forEach((sysvar, i) => {
+                    const code = auxVarParams.values[i * 3];
+                    const level = measureCodeMap[code];
+                    if (level) {
+                        sysvar.measurementLevel = level;
+                    }
+                });
+            }
             return meta;
         });
     }
diff --git a/node_modules/sav-reader/dist/SysVar.d.ts b/node_modules/sav-reader/dist/SysVar.d.ts
index a7e5d57..b7c5f98 100644
--- a/node_modules/sav-reader/dist/SysVar.d.ts
+++ b/node_modules/sav-reader/dist/SysVar.d.ts
@@ -10,6 +10,11 @@ export declare class SysVar {
     name: string;
     label: string;
     missing: any;
+    /**
+     * 'nominal' | 'ordinal' | 'scale', from the Auxiliary Variable Parameter
+     * info record. Undefined if the file has no such record.
+     */
+    measurementLevel?: string;
     /**
      * For internal use when reading data. Should probably find a better way to approach this.
      */
diff --git a/node_modules/sav-reader/dist/records/InfoRecord.js b/node_modules/sav-reader/dist/records/InfoRecord.js
index 2954287..e8bfa2f 100644
--- a/node_modules/sav-reader/dist/records/InfoRecord.js
+++ b/node_modules/sav-reader/dist/records/InfoRecord.js
@@ -61,9 +61,24 @@ class InfoRecord {
                 return rec;
             }
             else if (rec.subType === RecordType_js_1.InfoRecordSubType.AuxilliaryVariableParameter) {
-                // auxilliary variable parameter (one per system file)
-                // todo: this describes measurement, width, alignment for vars
-                return rec;
+                // Auxiliary variable parameter (one per system file): a flat
+                // array of int32s, 3 per variable, in dictionary order:
+                // [measurementLevel, displayWidth, alignment] repeated.
+                // measurementLevel: 1=nominal, 2=ordinal, 3=scale.
+                // alignment: 0=left, 1=right, 2=center.
+                // Verified against 25 real .sav files (including files with
+                // long string variables, which split into multiple internal
+                // variable records): count always equals
+                // (final logical variable count) * 3, never the raw
+                // variable-record count, so this aligns with the fully
+                // resolved variable list a consumer builds (post long-string
+                // continuation merging), not the raw dictionary records.
+                const values = [];
+                for (let i = 0; i < rec.count; i++) {
+                    values.push(getIntFromBuffer(byteData, i * rec.size));
+                }
+                const rec_ext = Object.assign(Object.assign({}, rec), { values });
+                return rec_ext;
             }
             else if (rec.subType === RecordType_js_1.InfoRecordSubType.LongVariableNamesRecord) {
                 // long variable names record
